{
  "schema": 1,
  "description": "Choose a model depending on the context",
  "execution_settings": {
    "default": {
      "max_tokens": 1000,
      "temperature": 0.5,
      "quiz_model": "gpt4o"
    }
  },
  "input_variables": [
    {
      "name": "input",
      "description": "Context that needs a model",
      "default": ""
    }
  ]
}